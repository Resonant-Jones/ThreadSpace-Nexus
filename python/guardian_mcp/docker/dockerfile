# Example for guardian-backend and mcp-server
FROM python:3.11

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

CMD ["uvicorn", "guardian_api:app", "--host", "0.0.0.0", "--port", "8000"]